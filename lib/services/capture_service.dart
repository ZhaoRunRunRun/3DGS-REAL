class CaptureService {
  const CaptureService();

  Future<void> prepareCameraGuide() async {
    // Placeholder for camera warm-up, permission checks, and guidance calibration.
    await Future<void>.delayed(const Duration(milliseconds: 300));
  }
}
